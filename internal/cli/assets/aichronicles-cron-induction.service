[Unit]
Description=aichronicles induction sweep run

[Service]
Type=oneshot
# %h expands to $HOME at runtime; same convention every other
# aichronicles unit uses for %h/.local/bin/. Keeps the unit
# user-portable without hardcoding /home/<user>/.
ExecStart=%h/.local/bin/aichronicles induction sweep
